services:
  polaris-postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: polaris
      POSTGRES_PASSWORD: polaris
      POSTGRES_DB: polaris
    ports:
      - "5432:5432"
    volumes:
      - polaris-pgdata:/var/lib/postgresql/data

volumes:
  polaris-pgdata:
